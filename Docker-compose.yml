version: '3.8'

services:
  appointments:
            image: abdullah144/appointments-app:2100afed72fc821430fd592263aa8584d50b052cs-app:c9337bb5b37578610b5b365599e55290ef0e5b15
    ports:
      - "7070:7070"
    networks:
      - app_network

  doctors:
    image: ${{ secrets.DOCKER_USERNAME }}/doctors-app:doctors
    ports:
      - "9090:9090"
    networks:
      - app_network

  frontend:
    image: ${{ secrets.DOCKER_USERNAME }}/frontend-app:frontend
    ports:
      - "3000:3000" 
    networks:
      - app_network
  #you said to assume it has db
  db:
    image: mysql:3.8
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: my_db
      MYSQL_USER: db_user
      MYSQL_PASSWORD: db_password
    networks:
      - app_network
networks:
  app_network:
    driver: bridge
